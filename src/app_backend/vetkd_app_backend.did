service : {
  "authorize" : (blob) -> ();
  "store_encrypted_document" : (blob) -> ();

  "list_my_documents" : () -> (vec blob) query;

  "encrypted_symmetric_key_for_caller" : (blob) -> (blob);
  "symmetric_key_verification_key" : () -> (blob);
};
